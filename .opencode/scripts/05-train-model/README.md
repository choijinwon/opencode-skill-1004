# 04 Train Model / Selected Model Build

Skill folder:
`../../skills/05-agent-mlflow-skill-train-model`

Scripts:

- `run_training.py`

Responsibility:

- 2번 `select_model.py`가 만든 작업 폴더의 `runtest_2.py` 실행
- `.env` 5개 값이 준비된 경우 원격 MLflow 등록 실행
